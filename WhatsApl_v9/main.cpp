#include "headers.h"


using namespace std;


int main(){

	string file;
	string login;
	string nome;
	string email;
	string nTelemovel;
	string dataAdesao;
	vector<Utilizador> utilizadores;
	vector<Grupo> machos;
	vector<Utilizador> machetes;
	vector<Mensagem> mens;

	// Primeira coisa que faz e ler o conteudo dos ficheiros para os vetores....



// Ver melhor como identificar a conversa!!!!!
// Como identificar os vetores dos respetivos grupos???


/*
	int valor;

	try{
		valor = menu1();
	}
	catch(RespostaNaoAceite<int> &f){
		cout << "Resposta invalida: " << f.getResposta() << endl;
	}

	system("cls");
	if (valor == 1){
		cout << "Utilizadores: " << endl;
		NomeUtilizadores(amigos);
	}
	else if (valor == 2){
		cout << "Grupos: " << endl;
		tituloGrupos(machos);
	}
	else if(valor == 3){
		int option;
		if (option != 1 || option != 2)
			throw RespostaNaoAceite<int>(option);
		cout  << "1. Registar Utilizador" << endl;
		cout  << "2. Registar Grupo" << endl;
		cout << "opcao: ";
		cin >> option;

		try{
			cin >> option;
		}
		catch(RespostaNaoAceite<int> &f){
			cout << "Resposta invalida: " << f.getResposta() << endl;
		}
	}
	else if (valor == 5){
		creditos();
	}

*/
//getch();

	return 0;
}

